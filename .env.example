# Public site origin used for sitemap.xml and robots.txt (no trailing slash).
NEXT_PUBLIC_SITE_URL=

# Approved endpoint that receives normalized lead payloads from /api/leads.
# If empty, the forms show a configuration error instead of pretending to submit.
LEAD_ENDPOINT=
