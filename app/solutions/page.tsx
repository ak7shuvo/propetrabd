import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("Solutions", "Explore PETRA solutions for hotels, resorts, boutique properties, guest houses and serviced apartments.", "/solutions");
import Link from "next/link";
import { ArrowRight } from "@/components/icons";
import { PageHero, SolutionCard, CTASection, SectionHeading } from "@/components/site";
import { solutions } from "@/lib/data";
export default function Solutions(){return <main id="main-content"><PageHero kicker="SOLUTIONS" title="Property management shaped around your operation." text="PETRA is designed for different hospitality environments while keeping the underlying product focused on connected property workflows."><Link className="btn primary" href="/product">Explore the Product <ArrowRight/></Link></PageHero><section className="section"><div className="container"><SectionHeading eyebrow="PROPERTY TYPES" title="One platform, different operating contexts." text="The product direction supports the workflows that matter across each property type without inventing separate products." align="center"/><div className="solution-grid solution-grid-five">{solutions.map((s,i)=><SolutionCard key={s.slug} title={s.title} text={s.summary} href={`/solutions/${s.slug}`} index={i}/>)}</div></div></section><section className="section soft-section"><div className="container split-copy"><div><span className="page-kicker">COMMON FOUNDATION</span><h2>Reservations, rooms, guests, teams and reporting stay connected.</h2></div><p>Each solution uses the same core PETRA product direction. What changes is the operational context in which those capabilities are used.</p></div></section><CTASection/></main>}
