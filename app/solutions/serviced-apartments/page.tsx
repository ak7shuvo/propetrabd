import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("Serviced Apartments", "PROPETRA for serviced apartment reservations, units, guests, billing and property operations.", "/solutions/serviced-apartments");
import Link from "next/link";
import { ArrowRight, Check } from "@/components/icons";
import { CTASection, PageHero } from "@/components/site";
import { solutions } from "@/lib/data";
export default function Solution(){const s=solutions.find(x=>x.slug==="serviced-apartments")!;return <main id="main-content"><PageHero kicker={`SOLUTIONS / ${s.title.toUpperCase()}`} title={s.summary} text={`A PROPETRA solution view for ${s.title.toLowerCase()}, built around the documented property-management workflow rather than unsupported promises.`}><Link className="btn primary" href="/product">See PROPETRA <ArrowRight/></Link><Link className="btn text-btn" href="/solutions">All Solutions <ArrowRight size={17}/></Link></PageHero><section className="section"><div className="container solution-detail-grid"><div><span className="page-kicker">OPERATIONAL FIT</span><h2>Keep the work around each stay in one property workspace.</h2><p>PROPETRA brings the same core PMS foundation into the context of {s.title.toLowerCase()}. The intent is to reduce scattered operational handoffs and give teams a shared view of property activity.</p></div><div className="solution-points">{s.points.map(p=><div key={p}><Check/><span>{p}</span></div>)}</div></div></section><section className="section soft-section"><div className="container"><div className="section-heading center"><span className="eyebrow"><span/>CORE WORKFLOW</span><h2>From reservation to reporting.</h2><p>Reservations, rooms, guests, housekeeping, billing, payments, staff and reports remain part of the same product architecture.</p></div><div className="workflow-ribbon">{["Reservations","Rooms","Guests","Housekeeping","Billing","Reports"].map((x,i)=><div key={x}><span>0{i+1}</span><strong>{x}</strong></div>)}</div></div></section><CTASection title={`Explore PROPETRA for ${s.title.toLowerCase()}.`}/></main>}
